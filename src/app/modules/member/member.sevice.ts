const creteMemberIntoDB = async (payload: any) => {
 
};
const getAllMembersFromDB = async () => {
 
};
const getMemberFromDB = async (memberId: string) => {
 
};
const updateMemberIntoDB = async (memberId: string) => {
 
};
const deleteMemberFromDB = async (memberId: string) => {
 
};

export const MemberServices = {
    creteMemberIntoDB,
    getAllMembersFromDB,
    getMemberFromDB,
    updateMemberIntoDB,
    deleteMemberFromDB
}